recorder {
    core {
        # mode = "Proxy"
        # encoding = "utf-8"
        # simulationFolder = ""
        # package = ""
        # className = "RecordedSimulation"
        # thresholdForPauseCreation = 100
        # saveConfig = false
        # headless = false
        # harFilePath = ""
        # format = "java8"
    }
    filters {
        # enable = false
        # allowList = []
        # denyList = []
    }
    http {
        # automaticReferer = true
        # followRedirect = true
        # removeCacheHeaders = true
        # inferHtmlResources = true
        # checkResponseBodies = false
        # useSimulationAsPrefix = false
        # useMethodAndUrisAsPostfix = false
    }
    proxy {
        # port = 8000
        https {
            # mode = "SelfSignedCertificate"
            keystore {
                # path = ""
                # password = ""
                # type = "JKS"
            }
            certificateAuthority{
                # certificatePath = ""
                # privateKeyPath = ""
            }
        }
        outgoing {
            # host = ""
            # username = ""
            # password = ""
            # port = 0
            # sslPort = 0
        }
    }
    netty {
        # maxInitializeLineLength = 10000
        # maxHeaderSize = 20000
        # maxChunkSize = 8192
        # maxContentLength = 100000000
    }
}